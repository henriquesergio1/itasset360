# Stage 1: Build the React Application
FROM node:20-alpine as build

WORKDIR /app

# Copy package.json and install dependencies
COPY package.json ./
RUN npm install

# Copy source code
COPY . .

# --- FIX STRUCTURE FOR REACT SCRIPTS ---
# Create standard CRA directories
RUN mkdir -p public src

# Move Static Assets to public/
RUN [ -f index.html ] && mv index.html public/
RUN [ -f metadata.json ] && mv metadata.json public/

# Move Source Code to src/
# We explicitly move folders and TS/TSX files.
# IMPORTANT: We DO NOT move 'server.js' so it stays in root and is IGNORED by react-scripts build.
RUN mv components contexts services utils src/ 2>/dev/null || true
RUN mv *.tsx *.ts src/ 2>/dev/null || true

# IMPORTANT: Set CI=false to treat warnings as warnings, NOT errors.
RUN CI=false npm run build

# Stage 2: Serve with Nginx
FROM nginx:alpine

# Remove default nginx static assets
RUN rm -rf /usr/share/nginx/html/*

# Copy static assets from builder stage
COPY --from=build /app/build /usr/share/nginx/html

# Copy custom nginx config
COPY nginx.conf.txt /etc/nginx/conf.d/default.conf

# Expose port 80
EXPOSE 80

# Start Nginx
CMD ["nginx", "-g", "daemon off;"]